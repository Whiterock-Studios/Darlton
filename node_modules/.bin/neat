#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../neat/bin/neat" "$@"
  ret=$?
else 
  node  "$basedir/../neat/bin/neat" "$@"
  ret=$?
fi
exit $ret
